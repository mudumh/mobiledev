<div class="container-fluid">
	
	
  	<%= render @tool  %>
  <div class= "row">
		<div class="col-md-2 ">
		<%= link_to 'Back', tools_path %>
		</div>
	</div>

<%= render 'comments/form'%>
		<%= render 'comments/comments'%>
