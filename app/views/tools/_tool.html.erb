<div class="row">
<%= link_to tool.url do %>
  <h3>
  	<%= tool.title%> 
  	<%= tool.get_upvotes.size %>
  	<%= link_to like_tool_path(tool), method: :put, class: "" do %>
    	<span class="glyphicon glyphicon-arrow-up"></span> 
    <% end %>
  </h3>
<% end %>
</div>

<div class = "row">
	<%=date_posted(tool.created_at)%>
	<span>
		 posted by:
		<%= link_to tool.user.username, user_path(tool.user) %>
	</span>
	
</div>